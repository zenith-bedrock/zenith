# Local dev: bind-mount ./deploy/data so you can edit zenith.yml on the host.
# Copy to docker-compose.override.yml (gitignored) or merge volumes manually.
services:
  zenith:
    volumes:
      - ./deploy/data:/data
