# Local dev: bind-mount ./deploy/data so you can edit zenith.yml on the host.
# From repo root with root compose, or copy next to deploy/compose/docker-compose.yml.
services:
  zenith:
    volumes:
      - ../data:/data
