# Local dev: bind-mount deploy/data so you can edit zenith.yml on the host.
# From repo root (with root docker-compose.yml include):
#   cp deploy/compose/override.example.yml docker-compose.override.yml
services:
  zenith:
    volumes:
      - ./deploy/data:/data
